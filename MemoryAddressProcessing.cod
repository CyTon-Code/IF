/*IF - это сочитание таких вещей: 1 адрес на исполняемый блок кода (возможно
функции), Умножение (bool)term на адрес.*/

/*ELSE - это тот же IF но с использованием такой вещи: NOT (Обратное 1 --> 0
0 --> 1).*/

/*NOT - Важный оператор(для работы с bool), Возвращает 1 если 0 и если 0 то 1.*/


// пример IF в псевдо-коде:

term = 1; // bool
address=*Некий_aдрес_функции*;
IF = address * term; // Некий aдрес функции
IF(); // Исполнится.


// пример IF и ELSE в псевдо-коде:

term = 1; // bool
AddressIF=*некий_адрес_функции_для_IF*;
AddressELSE=*некий_адрес_функции_для_ELSE*;
IF = AddressIF * term; // Некий ядресс функции для IF
ELSE = AddressELSE * NOT term; // 0
// нельзя так: "ELSE = NOT IF;". Потому что если IF = 0, то ELSE станет = 1.
IF(); // Исполнится.
ELSE(); // Не исполнится.


// пример NOT в псевдо-коде:

term = 1; // bool
term = abs(term -1); // |1 - 1| = 0
